---
target: the site (index + app + pitch)
total_score: 31
p0_count: 0
p1_count: 1
timestamp: 2026-07-14T14-57-18Z
slug: index-html
---
Method: dual-agent (A: design-director review · B: detector + browser overlay)

# Design Health Score — 31/40 (Good)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Math bar, toasts, echoes excellent, but the toast collides with the action bar |
| 2 | Match System / Real World | 4 | WA format, "Who's working?", plain English, road-sign brand |
| 3 | User Control and Freedom | 2 | Undo exists but its hit-target overlaps Build/Copy; form success is a dead-end without reload |
| 4 | Consistency and Standards | 3 | Chips/segs consistent; add-crew .seg stretches full-width leaving a dead half |
| 5 | Error Prevention | 3 | Past-date blocked, phone validated, build guards present |
| 6 | Recognition Rather Than Recall | 4 | Chips, crew list, DRIVER badges, number echo |
| 7 | Flexibility and Efficiency | 3 | Search, ±5min, extra-stop suggestion, backup, two pickup modes |
| 8 | Aesthetic and Minimalist Design | 4 | Clean, committed, not templated |
| 9 | Error Recovery | 3 | Superb inline copy ("That number doesn't look right…") |
| 10 | Help and Documentation | 2 | Only footnote + hints; no first-run orientation for Dee on the pre-filled demo |
| **Total** | | **31/40** | **Good — the gap is the toast collision + control/recovery, not visuals** |

# Anti-Patterns Verdict

**Not slop. Human-directed.** Both assessments agree: committed green/white/black, zero gradient-on-text, real pickup points and crew @-tags, honest "EXAMPLE" tags on placeholder quotes, brand discipline (green only on action/selected states). Template patterns present (hero over crowd photo, count-up stat tiles) but executed with restraint.

**Deterministic scan: clean** across all three pages. **Overlay:** the only genuine hit is `gpt-thin-border-wide-shadow` on `.form-card` (1.5px border + 20px shadow). Every low-contrast alarm (3 on index, 4 on pitch) was re-computed in-page as a false positive from the same oklch background-resolution bug: real ratios are ~5:1 (white on green door) and ~19:1 (price text on dark cards).

# Overall Impression

A feature-complete, brand-true build whose run-sheet output is genuinely the product. The score held roughly flat vs the 32/40 baseline only because a new toast-collision regression offsets real gains (WhatsApp submit, backup, home pickups, crew grid). Fix the toast and the polish items and it clears the mid-30s.

# What's Working

1. **The WhatsApp run-sheet output nails the product.** Dark WA bubble, teal @-tags, "now ✓✓", byte-for-byte Dee's format, plus the intelligent "Suggested new stop" callout with one-tap add.
2. **Form success cards turn the WhatsApp-handoff limit into reassurance** — number echo, explicit CV-attach steps, email fallback. Honest about the mechanic.
3. **The live math bar as consequence-preview** — "everyone seated ✓ (2 spare)" flips to orange "seats short. Add a driver" before Dee even builds.

# Priority Issues

- **[P1] App toast collides with the sticky action bar; Undo is an unreliable tap target.** The toast sits at bottom:24px, inside the action-bar band, so the Undo button renders on top of Build/Copy — a tap for Undo can fire Build. The recovery net for a destructive Clear fails exactly when it's needed. Fix: lift the toast above the action bar on the app page. Command: /impeccable polish
- **[P2] Mobile header wrap on index.html.** At 390px "Join the crew" wraps to three lines between logo and Hire staff, looking broken on first impression. Fix: nowrap the nav link and free width (drop the wordmark text to just the MJ box under ~430px). Command: /impeccable adapt
- **[P2] Dee lands on a pre-filled 13-crew demo with no "demo / start fresh" affordance.** Great theatre for Friday, but a paste-the-wrong-thing hazard for real use; the disclaimer is at the very bottom. Fix: a small "Demo crew loaded · Clear to start" note near the crew panel. Command: /impeccable onboard
- **[P3] Crew grid density at 390px** — two columns squeeze cards so "area · N shifts" wraps. Fix: single column under ~430px.
- **[P3] Add-crew polish** — Area presents 10 chips for one decision; the "Has a car?" .seg stretches full-width leaving a dead right half; the custom-crew ✕ is ~34px (<44px). Fix: width:max-content on the seg, pad the ✕.

# Persona Red Flags

**Jordan (first-time worker, phone):** the wrapped "Join the crew" is a broken-looking first impression; the CV two-step (send, then attach) could be missed despite the clear note.
**Casey (one-handed):** the toast/action-bar overlap is the killer — a thumb aiming for Undo lands on Build/Copy.
**Riley (stress tester):** refresh mid-flow is safe; "From homes" at clustered addresses can stamp two sequential stops the same time (5-min rounding) which reads odd; the Undo collision.
**Dee (non-technical, <1 min):** genuinely fast, but a fat-finger Clear may not be recoverable first try; and the pre-filled demo risks being pasted into a real group.

# Minor Observations

- Footer Instagram is a dead "#" (labelled coming soon).
- Desktop: empty green whitespace above the contact bar after the crew form submits (success card shorter than the sticky column).
- Three "EXAMPLE" testimonials are honest but can read to an owner as "no real reviews yet."
- Pitch count-up final values land correctly (0, 75%, 6,000+, 592); no 390px overflow.

# Questions to Consider

1. Is a populated demo the right default for Dee's live first use, or does it want an explicit demo/reset mode?
2. The Formspree endpoint is already stubbed — is a real backend worth Phase 1 so CVs actually arrive?
3. Would a single real D&J line outperform all three "EXAMPLE" testimonials?
